# Python package
# Create and test a Python package on multiple Python versions.
# Add steps that analyze code, save the dist with the build record, publish to a PyPI-compatible index, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/python

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.11'
  displayName: 'Use Python 3.11'

- script: |
    python -m pip install --upgrade pip
    pip install -r requirements.txt
  displayName: 'Install dependencies'

- script: |
    pip install pytest pytest-azurepipelines
    pytest
  displayName: 'pytest'

- script: |
    git config --global user.name "sdokalova"
    git config --global user.email "sofiya.dokalova@quantori.com"
    git remote set-url origin https://$5lqb5urbvyqmtprd7psjxcdz7tqcbv7lqcateyrswt2zgxczwgza@github.com/sdokalova/restful-booking-for-github-actions.git
    git clone https://$5lqb5urbvyqmtprd7psjxcdz7tqcbv7lqcateyrswt2zgxczwgza@github.com/sdokalova/restful-booking-for-github-actions.git
    cd your_repo_name
    git checkout -B gh-pages
    cp -R ../allure-results/* .
    cp -R ../gh-pages/history/* .    # Get Allure history
    git add .
    git commit -m "Update Allure report"
    git push -f origin gh-pages
  env:
    GH_TOKEN: $5lqb5urbvyqmtprd7psjxcdz7tqcbv7lqcateyrswt2zgxczwgza
  displayName: 'Push Allure reports to gh-pages'